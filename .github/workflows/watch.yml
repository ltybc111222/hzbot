- name: save state
        if: always()
        run: |
          git config user.name  "hzbot"
          git config user.email "hzbot@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || git commit -m "state"
          git push || true
